# Data Catalog

데이터를 코드에 사용하기 전에 이 문서에 출처, 시간 범위, 단위, 주요 변수, 사용 목적, 검증 상태를 기록한다.

상태는 `VERIFIED`, `PARTIAL`, `UNVERIFIED` 중 하나를 사용한다.

> 아래 실측 수치는 2026-09-13 데이터 audit에서 로컬 raw 파일을 직접 읽어 확인한 값이다.
> 실측 수치는 **참고용 검증 결과**이며, 이를 근거로 내린 **공식 결정**은 `DECISIONS.md`에만 기록한다.
> audit 검증 스크립트는 W1 전처리 구현 시 `outputs/audit/`로 이관하여 재현 가능하게 만든다.

## 요약

| 데이터 | 용도 | 시간 범위 / 단위 | 상태 | 비고 |
|---|---|---|---|---|
| 인허가 3종 (일반음식점·휴게음식점·미용업) | **주 폐업 라벨**, 개·폐업 시점, 모집단 | ~2026-09 / 사업체 | VERIFIED | 폐업일자 일 단위, 신고 지연 존재 |
| 소진공 상가(상권)정보 | 사업체 위치·업종 feature, 라벨 **보조정보** | 2024-12~2026-06, 7개 스냅샷 / 사업체 | VERIFIED | 소멸은 폐업 라벨로 사용 금지. **202503 좌표는 배포본 전체가 이상**(§2-2) |
| 서울 상권분석서비스 (매출·점포·변화지표·인구) | 상권 단위 수요·경쟁 feature | 2021Q1~2026Q2, 22분기 / 상권 | PARTIAL | 상권코드 집합 전 기간 동일 확인, polygon 경계 일관성은 미검증 |
| 상권 영역 shp | 공간 조인 | 스냅샷 / 상권 폴리곤 | VERIFIED | EPSG:5181 |
| 개별공시지가 | 비용 환경 feature | 2024·2025·2026 / 필지(PNU) | VERIFIED | 기준일과 공시일 구분 필요 |
| MDIS 소상공인실태조사 2023 | Stage 3 인과분석(DML) | 2023 / 사업체(전국) | VERIFIED | 시도까지만, 자치구 없음 |
| 네이버·카카오 온라인 정보 | 블로그·카페 언급(과거 재구성 가능) / 등록·순위(현재 진단용) | 수집 시점 | UNVERIFIED | W1 ④에서 수집 후 갱신 |

## 1. 인허가 3종 (공공데이터포털, CP949)

- 파일: `data/00_raw/인허가/`
  - 일반음식점: `서울시 일반음식점 인허가 정보.csv` (537,517행) — **정정(실측 확인, 2026-09-18):
    파일명이 "식품_일반음식점.csv"로 잘못 기록되어 있었다. 실제 파일명은 위와 같다.**
  - 휴게음식점: `식품_휴게음식점.csv` (646,569행)
  - 미용업: `생활_미용업.csv` (458,647행)
- 3구(광진·마포·영등포) 필터 실측 — **모집단 정본은 `개방자치단체코드` 기준**(`DECISIONS.md` 2026-09-21):
  일반음식점 76,451 / 휴게음식점 20,483 / 미용업 13,413 = 합계 110,347.
  주소텍스트 기준(QA용 대조값)은 76,453 (영업 18,865 / 폐업 57,588) / 20,484 / 13,418 = 합계 110,355.
- 식별자: `관리번호` — 3구 범위에서 중복 0건(3개 파일 합산 기준으로도 0건).
- 폐업일자: 폐업 건에서 결측 0. **함정: 영업 중 행의 폐업일자가 NaN이 아닌 공백 문자열인 경우가 있어 `strip()` 후 결측 처리 필요.** 미래 날짜는 0건(과거 "3건" 보고는 공백 문자열의 오인).
- 3구 폐업 연도 분포(일반음식점): 2023년 2,500 / 2024년 2,686 / 2025년 2,932. 3업종 합산 연 약 3,800~4,500건. (초기 계획서의 14,648~16,596은 다른 집계 범위의 수치로 확인됨 — 사용 금지)
- 2026년 폐업 건수는 당월(부분월)만 과소 집계된다 — **정정(실측 확인, 2026-09-18): "최근 수개월이
  과소 집계된다"고 기록했으나, 완결된 최근 5개월은 trailing baseline 대비 0.78~1.74로 정상 등락하고
  flag 0건이었다.** 다개월 신고 지연은 관측되지 않는다 → 성숙 컷오프 1개월(`DECISIONS.md` 2026-09-18).
- 좌표: EPSG:5174, 결측 제외 100%가 서울 범위. 결측률은 업종별 상이 — 일반음식점 10.6%, 휴게음식점 4.7%, 미용업 4.9%.
- **인코딩 함정: 일반음식점 파일은 CP949로 완전 디코딩되지 않는 바이트가 존재**(byte offset 약 3,855,938 부근에서 실제 확인) → 로딩 시 `encoding_errors` 정책 필요. 미용업·휴게음식점은 전체 파일을 청크 단위 strict 디코딩 스캔한 결과 오류 0건(실측 확인, 2026-09-18).
- **컬럼 구성·순서가 3개 파일에서 서로 다름** — 일반음식점 39열, 휴게음식점 39열, 미용업 37열
  (**정정(실측 확인, 2026-09-18): 휴게음식점을 37열로 잘못 기록했었다. 실제로는 39열이다.**)
  → 위치 기반 접근 금지, 컬럼명 매핑표 사용.
- **3구 필터 기준: 모집단 포함/제외는 `개방자치단체코드`로 결정하고, 주소텍스트는 검증(QA)에만 쓴다**
  (`DECISIONS.md` 2026-09-21 "3구 모집단 정의 기준"). 두 기준의 실측 차이는 아래와 같다.

  | 기준 | 일반음식점 | 휴게음식점 | 미용업 | 합계 |
  |---|---|---|---|---|
  | `개방자치단체코드` (정본) | 76,451 | 20,483 | 13,413 | **110,347** |
  | 주소텍스트 (QA 대조) | 76,453 | 20,484 | 13,418 | 110,355 |
  | 교집합 | 76,448 | 20,481 | 13,412 | 110,341 |

  불일치는 총 20건(주소텍스트에만 14 / 코드에만 6)이다. 3개 파일에서 이 20건을 직접 대조한
  결과 지번주소는 명백히 마포구/광진구/영등포구인데 코드가 다른 값이거나, 반대로 코드는 3구인데
  주소가 서대문구·구로구·성북구·용산구·충남 논산시·인천 부평구인 행이 섞여 있었다. **어느 쪽
  필드에 오류가 있는지는 원본만으로 판정할 수 없으므로, 재현성이 높은 구조화 필드(코드)를
  기준으로 삼고 주소 불일치는 삭제 대신 `gu_mismatch` 플래그로 보존한다**(현재 실측 4건,
  `outputs/standardized/qa_report.md`). 2026-09-13 audit의 3구 실측치(76,453/20,484/13,418)는
  주소텍스트 기준 값이며, 위 표의 QA 행에 해당한다.
- 한계: 인허가일자는 개업일 proxy(승계·양도양수 시 업력 왜곡 가능), 이전(移轉)은 식별 불가.

## 2. 소진공 상가(상권)정보 (공공데이터포털, UTF-8)

- 파일: `data/00_raw/소진공_상가정보/` — **7개 스냅샷: 202412, 202503, 202506, 202510, 202512, 202603, 202606** (간격 2~4개월, 불균일).
- 3구 행수: 79,285 → 77,828 → 77,049 → 76,898 → 76,943 → 77,083 → 80,404. 전 스냅샷 업소번호 중복 0건.
- `지번코드` = PNU 19자리. 법정동코드(10)+대지구분(1)+본번(4)+부번(4) 공식과 7개 스냅샷 전수 100% 일치. 같은 업소번호의 지번코드 변경은 0.9%로 안정적.
- 결측: 호정보·동정보 100%, 층정보 약 30%, 지점명 약 90%. → 결합 키는 PNU+정규화 상호(+좌표 보조).
- 연속 스냅샷 유지율(3구): 93.5% / 95.3% / 94.3% / 97.3% / 96.9% / 89.8%. 부활(중간 공백 후 재등장)은 전체 고유 업소번호의 0.6%.
- **202603→202606 구간 이상**: 소멸 7,845(월 2,615) + 신규 11,166(월 3,722)으로 급증. 완전 신규(이전 6개 스냅샷 부재) 11,014건. **소멸 중 30.5%(2,393건)는 동일 필지+동일 정규화 상호가 새 업소번호로 재등장 = ID 재발급/DB 정비.** 이 구간의 소멸·신규는 실제 폐업·개업으로 해석 금지.
- 인허가 폐업(3구 일반음식점, 폐업일 2025-01~2026-06, n=4,004)과의 대조 실측:
  - 소진공 매칭 성공 55.5% (영업 중 점포는 80.1% — 매칭 실패가 폐업과 상관, complete-case 금지 근거)
  - 매칭 성공분 중: 동일 관측구간 소멸 72.0% / 선행 소멸 13.9%(갭 중앙값 6개월) / 폐업 후 잔존 12.9%(중앙값 9개월, 최대 18개월) / 미확인 1.2%
- 결론: 소멸은 폐업 라벨 원천으로 부적합(커버리지·ID 오염·시점 해상도). 라벨 보조정보(`sj_status`, `sj_gap_months`)로만 사용 — `DECISIONS.md` 2026-09-13 참조.

### 2-1. ER 산출물과 B-3 handoff (2026-09-19)

`python -m src.data.matching`이 생성한다 (모두 git 미추적). **ER은 `sj_status`를 만들지 않는다.**
B-3가 아래 산출물로 파생한다. 소진공 소멸은 주 폐업 라벨이 아니며 주 라벨은 인허가 `close_date`다.

| 파일 | 단위 | B-3가 쓸 컬럼 |
|---|---|---|
| `outputs/matching/license_semas_matches.parquet` | 인허가 점포 (110,347행 전체) | `store_id`, `matched`, `ambiguous`, `sj_entity_id`, `sj_store_id`(매칭 당시 후보 ID), `match_tier`, `match_confidence`, `crowded_pnu`, `name_structure`, `unmatched_reason`, `license_date`, `close_date`, `status_name` |
| `outputs/standardized/semas_entities.parquet` | 소진공 업소번호 (105,264행) | `sj_store_id`, `sj_entity_id`, `id_reissued`, `entity_n_ids`, **`entity_first_snapshot`, `entity_last_snapshot`**, `entity_latest_sj_store_id` |
| `outputs/standardized/semas_panel.parquet` | 업소번호 × 스냅샷 (545,490행) | `snapshot`, `sj_store_id` (+ 표준화 속성). `sj_entity_id`는 없으므로 entities와 `sj_store_id`로 조인 |

- **B-3는 SEMAS 존재구간·last_seen을 반드시 `sj_entity_id` 기준으로 계산한다.**
  entities의 `entity_first_snapshot`/`entity_last_snapshot`을 그대로 쓰거나, 스냅샷별 이력이 필요하면
  `panel ⋈ entities (sj_store_id)` 후 `sj_entity_id`로 묶는다.
  **업소번호 기준 `first_snapshot`/`last_snapshot`은 쓰지 않는다** — 재발급된 4,700개 업소번호가 소멸처럼 보인다.
- 재발급: unambiguous 1:1 링크만 한 entity로 묶었고(`id_reissued=True`), 재발급 전후를 다시 나누지 않는다.
  ambiguous 링크 132건은 병합하지 않았으므로 각자 별도 entity다.
- 매칭 구분: `matched=False & ambiguous=True`는 후보가 여럿이라 확정하지 않은 행, `matched=False & ambiguous=False`는
  미매칭이다. 사유는 `unmatched_reason`(예: `tier4_name_not_exact_or_contained`, `ambiguous_candidates`).
- 신뢰도: `match_confidence` high(Tier1·2) / medium(Tier3) / low(Tier4). Tier3 중 `crowded_pnu=True`(후보 51개 이상 PNU)는
  표본상 오매칭 위험이 높아 후속 단계에서 별도 sensitivity 집합으로 비교할 수 있도록 flag를 보존했다.
  ER에서는 자동 제외·강등하지 않는다 (`DECISIONS.md` 2026-09-19).
- 시점 정합: **ER은 인허가 영업기간과 entity 관측구간의 겹침을 검사하지 않으며, 겹침이 짧다는 이유로
  매칭을 미매칭으로 바꾸지 않는다.** 필요하면 B-3에서 `license_date`/`close_date`와
  `entity_first/last_snapshot`으로 `overlap_days` 같은 값을 QA/provenance로 계산할 수 있다.
  현재 매칭 중 겹침 90일 미만은 6,105건이며, SEMAS 관측이 2024-12~2026-06이라 최근 개업 점포는 겹침이
  짧게 잡히는 경계 효과가 있다. `LABEL_SPEC.md`의 90일 겹침 조건과의 차이는 B-3 담당자와 별도 합의 사항이다.
- 202503 스냅샷 경위도는 전량 서울 밖이라 entity 좌표·Tier4 매칭에 쓰지 않는다 (entity 좌표 출처 중 202503은 0건). 원인 검증은 §2-2.

### 2-2. 202503 스냅샷 좌표 이상 (2026-09-20 재검증, Issue #14)

재다운로드한 202503 원본으로 다시 검증했다. 좌표는 원본 `경도`·`위도` 값을 그대로 읽었다.
상세 수치·figure는 `outputs/qa/semas_202503_coord_anomaly_summary.md`(git 미추적)에 있다.

- **로컬 파일 손상 아님**: 재다운로드본(`…_20250331` 배포 묶음의 서울 파일)과 `data/raw`의 202503이
  바이트 단위로 동일하다 (294,546,038 bytes, SHA256 `1c3d8ee0484b0fb668b83ae4f69a83e22e77b0645b943d80d630198418031047`).
- **파싱·열 밀림 아님**: UTF-8(BOM 없음) strict decode 성공, 39열 헤더가 202506과 동일, header와 필드 수가 다른 행 0,
  경도·위도 결측·파싱 실패 0, 코드 컬럼(시도·시군구·행정동·법정동·지번코드 등) 형식이 202506과 같은 수준으로 정상.
- **스냅샷 전체의 좌표 이상**: 서울 540,516행의 좌표가 100% 서울 범위 밖이다(경도 128.05~128.47, 위도 38.32~38.59).
  서울 25개 구 모두 같다. 같은 배포 묶음의 전국 17개 시도 202503 파일도 모두 같은 방식으로 밀려 있다.
- **좌표만 이상하고 식별·주소는 정상**: 202506과 공통인 업소번호 517,639건에서 시군구·법정동·지번코드·지번주소·
  도로명주소·업종 소분류가 100%, 상호명이 99.99% 일치한다. 같은 업소번호의 좌표는 전부 약 150km
  (149.9~150.2km) 떨어져 있다.
- 대조군: 202412와 202506의 공통 업소번호 499,680건은 좌표가 100% 완전히 같다. 정상 스냅샷 사이에서는 좌표가 움직이지 않는다.
- **이동 형태**: 고정 평행이동이 아니다(위도 차이가 경도와 상관계수 −0.9998). 202506 좌표의 affine 변환
  (약 150km 북동 이동 + 약 1% 늘림·기울어짐)으로 거의 완벽히 설명된다(R² ≈ 0.999999997, 잔차 약 1m 이하).
- **우리 쪽 CRS 오해 가능성 낮음**: 값이 도(degree) 단위 크기이고, 투영 좌표(EPSG:5174/5179/5181/5186의 미터)로
  재해석하면 무의미한 위치가 나와 기각했다. 같은 컬럼을 202412·202506은 경위도로 읽어 정상이다.
- **미확정**: 배포처에서 어떤 처리가 이 변환을 만들었는지는 데이터만으로 특정할 수 없다. 배포 묶음에 동봉된
  `[필독]파일열람방법.txt`에는 인코딩 안내만 있고 좌표 관련 공지는 없다. 배포처 공지·정정 이력은 확인하지 않았다.
- 주의: 밀린 좌표도 대부분 한반도 대략 범위 안에 떨어진다(시도별 포함률 강원 62%, 나머지 99~100%).
  **전국 단위 범위 검사로는 이 이상을 잡을 수 없다.** 서울 외 지역을 다룰 때는 시도별 범위 검사나
  스냅샷 간 좌표 일관성 검사가 필요하다.

## 3. 서울 상권분석서비스 (열린데이터광장, CP949)

- 추정매출·점포: 2021Q1~2026Q2 22개 분기 전부 존재. 상권변화지표: 36,300행 = 1,650상권 × 22분기.
- **상권코드 집합의 동일성 확인됨**: 점포·추정매출·상권변화지표 CSV에서 상권코드 1,650개가
  2021~2026 전 기간 완전 동일 (코드 추가·삭제·교체 0건 — 전수 확인).
- **미검증**: 위는 코드 집합의 동일성만 확인한 것이며, 연도별 polygon geometry(공간 경계)가
  동일하다는 검증은 아니다. 영역 shp는 단일 스냅샷만 보유하므로 2024년 표준단위구역 변경이
  경계 조정을 동반했는지는 별도 검증 대상 — 공간 조인 결과를 특정 연도 경계로 전 기간에
  적용할 때는 이 한계를 명시한다.
- 길단위인구·상주인구·직장인구·집객시설 포함. 영역 shp는 EPSG:5181 (.prj 확인).

## 4. 개별공시지가 (2024·2025·2026)

- 서울 25개구 전체, 연 약 89만 필지. 3구 약 10.9~11.0만 필지/년.
- `토지코드` = 19자리 PNU → 소진공 지번코드와 직접 조인 가능. 단 17~18자리 불량 코드가 연 약 200건 존재 → 제외+플래그.
- **시점 주의: 기준년월은 1월 1일이나 개별공시지가의 실제 결정·공시는 통상 4~5월** → feature 사용 시 available_at은 공시일 기준으로 태깅.

## 5. MDIS 소상공인실태조사 2023 (CP949)

- 40,000행 × 156열. 산업중분류 47·56·96 필터 = 5,042건, 그중 서울 474건.
- 처치: 경영_전자상거래_매출실적여부 — 실적 있음 857(17.0%) / 없음 4,185(83.0%). 매출비율은 실적군에서 90~100%에 집중.
- 결과: 경영_영업이익 결측 0%, 적자 5.5%.
- 가중치: 사업체수가중값 1종만 제공. 지역은 시도(17개)까지 — 자치구 불가, 전국 분석 + 서울 부분집합 평가.

## 6. 네이버·카카오 온라인 정보 (미수집, W1 ④)

- 블로그·카페 검색: **작성일이 존재하므로 월별 시계열로 재구성하면 과거 시점 feature로 사용 가능** — 수집 시 store_id × month 형태로 저장.
- 지역검색 등록 여부·순위, 카카오 로컬 등록 여부: 수집 시점의 현재값 → **현재 진단 표시용으로만 사용, 과거 예측 feature 사용 금지**.
- 모든 수집 레코드에 `collected_at`, `query_used` 기록 필수. 수집 후 매칭률과 함께 이 문서 갱신.

## 포기·대체 (참고)

- SGIS 100m 격자 인구 → 상권분석의 상주·직장·길단위인구로 대체.
- 한국부동산원 임대동향 → 개별공시지가로 대체.
- 골목형상점가(지정 연월 부재) → 인과분석 제외, 통제변수로만.
- 네이버 플레이스 평점·리뷰 크롤링 → 약관 리스크로 포기, 블로그·카페 검색 API로 대체.

## 등록 규칙

새 데이터셋을 추가할 때 최소한 다음을 확인한다: 원 출처, 다운로드/접근일, 관측 단위, 시간 범위, 공간 단위, 주요 변수 정의, 결측률, 다른 데이터와의 매칭 방식, 라이선스·이용 조건.
