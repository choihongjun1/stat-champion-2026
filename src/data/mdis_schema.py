"""MDIS 소상공인실태조사 2023 컬럼 매핑과 값 인코딩 상수.

`docs/W1_MDIS_AND_LABEL.md` A-3 3번 표 + 실제 CSV 헤더 매칭 결과 + 파일설계서
(`2023년_소상공인실태조사_연간자료_등록기반_파일설계서.xlsx`) 코드정보 시트 확인 결과를
코드의 단일 출처로 고정한다. 컬럼명 위치 인덱싱은 사용하지 않는다.
"""

ROLE_COLUMNS = {
    "outcome": ["경영_영업이익", "경영_매출금액"],
    "treatment": ["경영_전자상거래_매출실적여부", "경영_전자상거래_매출비율"],
    "covariate": [
        "산업대분류코드",
        "산업중분류코드",
        "행정구역시도코드",
        "일반_창업인수승계_연도",
        "일반_창업인수승계_월",
        "일반_창업형태코드",
        "일반_합계종사자수",
        "경영_영업비용_임차료",
        "경영_부채여부",
        "경영_판매처별매출_소비자비율",
    ],
    # "_코드"(중요도 척도) 버전 대신 "_여부"(이진) 버전을 사용한다.
    # 처치 후보 변수는 DML 설계상 이진이 다루기 쉽고, 척도형(중요도)은 별도 분석이 필요하기 때문.
    "lever_candidate": [
        "창업_준비활동_사업계획서작성여부",
        "창업_준비활동_시장조사여부",
        "창업_준비활동_동종업종종사경험여부",
        "창업_준비활동_창업교육여부",
    ],
    "weight": ["사업체수가중값"],
}

# 산업중분류코드 필터 대상 (문자열, zero-padding 없음 — CSV 실측 확인됨: '047' 형태는 0건)
INDUSTRY_CODES = ["47", "56", "96"]
EXPECTED_INDUSTRY_FILTERED_ROWS = 5042

# 원본 CSV를 dtype=str로 읽기 때문에(코드형 컬럼의 zero-padding·문자열 비교를 보존하기 위해)
# 실제로는 숫자인 컬럼도 object로 남는다. 이 목록은 명시적으로 숫자형 변환이 필요한 컬럼이다.
NUMERIC_COLUMNS = [
    "경영_영업이익",
    "경영_매출금액",
    "경영_영업비용_임차료",
    "일반_합계종사자수",
    "경영_판매처별매출_소비자비율",
    "경영_전자상거래_매출비율",
    "사업체수가중값",
]

# 여부 계열 컬럼 중 0/1 이진 변환이 필요한 컬럼.
# 원본 컬럼은 보존하고, 변환값은 "{컬럼명}_bin" 컬럼으로 별도 생성한다.
YESNO_COLUMNS = [
    "경영_부채여부",
    "창업_준비활동_사업계획서작성여부",
    "창업_준비활동_시장조사여부",
    "창업_준비활동_동종업종종사경험여부",
    "창업_준비활동_창업교육여부",
]

# 아래 값은 파일설계서 코드정보 시트에서 실측 확인한 값이다 (전 항목 공통: 1=예/수행, 2=아니오/미수행).
# 경영_전자상거래_매출실적여부도 동일 규칙이며, CSV 실측으로 분포(1=857, 2=4185)까지 교차검증했다.
YESNO_YES_VALUE = "1"
YESNO_NO_VALUE = "2"

ECOMMERCE_YES_VALUE = "1"
EXPECTED_TREAT_COUNTS = {1: 857, 0: 4185}

# 행정구역시도코드 실측: 서울='11' (문자열, zero-padding 없음). CSV 실측 n=3780.
SEOUL_CODE_VALUE = "11"
EXPECTED_SEOUL_COUNT = 474

# 결측이 있으면 안 되는 컬럼(M6) — 가중치는 모든 분석(가중 평균 등)에 필수라
# 하나라도 결측이면 그 행 전체를 조용히 계산에서 빠뜨리게 된다.
REQUIRED_NON_MISSING_COLUMNS = ["사업체수가중값"]

# PR #15 리뷰(choihongjun1): 가중치 <= 0이 결측 검증만으로는 잡히지 않던 문제.
# 가중치는 표본을 모집단으로 확장하는 용도라 0 이하면 그 행이 가중 통계에서
# 사라지거나(0) 부호가 뒤집히는(음수) 오류가 된다 — 결측 여부와 별개로 값 자체의
# 유효성을 검증해야 한다. 0 자체도 무효로 배제한다(포함하지 않음).
WEIGHT_MIN_VALUE = 0

# 다항(2값 초과) 코드 컬럼의 유효값 집합. recode_yesno_columns가 이진 컬럼을
# 다루는 것과 짝을 이루는 다항 코드 버전 — validate_categorical_code_columns가
# 이 dict를 순회하며 결측/이상값을 검증한다. 향후 다른 다항 코드 컬럼이 추가되면
# 이 dict에 항목만 늘리면 된다.
# 일반_창업형태코드: 1=신규창업, 2=인수창업, 3=가업승계 (파일설계서 코드정보 시트 확인 —
# VARIABLE_DEFINITIONS의 같은 컬럼 설명과 근거가 같다).
CATEGORICAL_CODE_COLUMNS: dict[str, set[str]] = {"일반_창업형태코드": {"1", "2", "3"}}

# 처치군 매출비율은 퍼센트이므로 이 값을 초과하면 인코딩이 깨졌다는 신호다.
TREAT_RATIO_MAX = 100

# 창업연도 sentinel: CSV 실측 결과 1900 다음으로 낮은 값은 1962 (62년 공백, 1901~1961 값 0건) —
# 자연 분포와 단절되어 있어 "모름/미상"을 뜻하는 sentinel로 판단(파일설계서에 별도 코드 명시 없음,
# 실측 근거에 따른 판단). MIN_PLAUSIBLE은 실측 최솟값(1962)보다 충분히 낮은 안전 하한으로,
# 향후 데이터 갱신 시 유사한 이상값을 조기에 탐지하기 위한 값이다.
TENURE_YEAR_SENTINEL = 1900
TENURE_YEAR_MIN_PLAUSIBLE = 1945
TENURE_YEAR_MAX = 2023

# tenure_months 한계 — 코드북 "정의"란에도 동일 문구를 사용한다.
TENURE_MONTHS_CAVEAT = (
    "일반_창업인수승계_연도/월 기반. 인수·승계 사업체의 경우 점포 자체의 개업 시점이 아니라 "
    "현재 사업자가 운영을 시작한 시점을 반영한다. 즉 tenure_months는 '점포 업력'이 아니라 "
    "'현재 사업자의 운영기간'으로 해석해야 한다 (DATA_CATALOG.md가 인허가일자를 "
    "'개업일 proxy(승계·양도양수 시 업력 왜곡 가능)'라고 명시한 것과 동일한 종류의 한계)."
)

_YESNO_DEF = "원본값 1=수행/2=미수행(파일설계서 코드정보 시트 확인)."
_YESNO_DEF_YN = "원본값 1=예/2=아니오(파일설계서 코드정보 시트 확인)."
_NUMERIC_NOTE = (
    "원본 CSV를 dtype=str로 읽어 object로 남는다 — 쉼표·공백 등 포맷 문제는 실측 결과 없음"
    "(N=5042 전량 단순 숫자 패턴). coerce_numeric_columns()로 숫자형 변환."
)

# build_codebook_rows가 참조하는 "정의" 텍스트. 컬럼을 추가하면 반드시 이 dict에도 정의를 추가한다
# (누락 시 build_codebook_rows에서 즉시 에러 — TODO로 비워두지 않는다).
VARIABLE_DEFINITIONS = {
    "경영_영업이익": f"결과변수. 영업이익 원본값. 적자(음수) 제거하지 않음 — 필터 후 N=5042 중 277건(5.5%) 음수, DATA_CATALOG.md §5 수치와 일치 확인. {_NUMERIC_NOTE}",
    "경영_매출금액": f"결과변수. 매출금액 원본값. profit_margin의 분모. 필터 후 0값 0건(N=5042 실측). {_NUMERIC_NOTE}",
    "경영_전자상거래_매출실적여부": f"처치변수(이진) 원본. {_YESNO_DEF_YN} CSV 실측 분포 1=857/2=4185 — DATA_CATALOG.md §5(17.0%/83.0%)와 일치.",
    "경영_전자상거래_매출비율": f"처치변수(연속) 원본. 실적 없음(여부=2)일 때 원본값이 NaN(CSV 실측: 여부=2인 4185건 전량 비율 NaN). treat_cont 생성 시 명시적 0으로 대체 — 결측이 아니라 '0%'라는 사실 자체가 정보이기 때문. {_NUMERIC_NOTE}",
    "산업대분류코드": "공변량. 산업 대분류(문자 코드, 예: I=숙박·음식점업).",
    "산업중분류코드": f"공변량 겸 필터 기준. {', '.join(INDUSTRY_CODES)} 값만 남긴다(문자열, zero-padding 없음 — '047' 형태로는 0건). 필터 후 N={EXPECTED_INDUSTRY_FILTERED_ROWS}.",
    "행정구역시도코드": f"공변량. 시도 단위 지역코드(문자열). 서울='{SEOUL_CODE_VALUE}'. is_seoul 파생의 기준.",
    "일반_창업인수승계_연도": f"tenure_months 계산 원천. 필터 후 결측 0건(N=5042 실측). 값 {TENURE_YEAR_SENTINEL}은 sentinel로 간주해 NaN 처리(tenure_invalid_flag 참조). " + TENURE_MONTHS_CAVEAT,
    "일반_창업인수승계_월": "tenure_months 계산 원천. 필터 후 결측 0건(N=5042 실측). " + TENURE_MONTHS_CAVEAT,
    "일반_창업형태코드": "공변량. 창업 방식(1=신규창업, 2=인수창업, 3=가업승계 — 파일설계서 코드정보 시트 확인). "
    "인수·승계 사업체는 tenure_months가 '점포 업력'이 아니라는 한계의 근거가 되는 구분 변수.",
    "일반_합계종사자수": f"공변량. 대표자 포함 총 종사자수. {_NUMERIC_NOTE}",
    "경영_영업비용_임차료": f"공변량. 연간 임차료 비용. {_NUMERIC_NOTE}",
    "경영_부채여부": f"공변량(이진) 원본. {_YESNO_DEF_YN} `경영_부채여부_bin`으로 변환.",
    "경영_판매처별매출_소비자비율": f"공변량. 최종소비자 대상 매출 비율(%). {_NUMERIC_NOTE}",
    "창업_준비활동_사업계획서작성여부": f"보조 레버 후보(이진) 원본. {_YESNO_DEF} `_bin`으로 변환. 결측 다수(N=27,180/40,000, 전체 모집단 기준) — 창업 준비활동 문항 자체의 skip 패턴으로 추정, A-4에서 결측 처리 방식 확정 필요.",
    "창업_준비활동_시장조사여부": f"보조 레버 후보(이진) 원본. {_YESNO_DEF} `_bin`으로 변환. 결측 패턴은 사업계획서작성여부와 동일.",
    "창업_준비활동_동종업종종사경험여부": f"보조 레버 후보(이진) 원본. {_YESNO_DEF} `_bin`으로 변환. 결측 패턴은 사업계획서작성여부와 동일.",
    "창업_준비활동_창업교육여부": f"보조 레버 후보(이진) 원본. {_YESNO_DEF} `_bin`으로 변환. 결측 패턴은 사업계획서작성여부와 동일.",
    "사업체수가중값": f"가중치. MDIS에서 제공하는 유일한 가중치(DATA_CATALOG.md §5). 가중 결과를 기본으로 하되 무가중 결과를 병기한다(DECISIONS.md 2026-09-13). {_NUMERIC_NOTE}",
    "mdis_row_id": "행 식별자. 원본 CSV의 0-based 행 위치(필터 이전 기준). 원본에 ID 컬럼이 없어 도입 — "
    "stage_a 내 유일성, stage_b ⊆ stage_a 관계를 검증하는 기준 key.",
    # 파생변수
    "tenure_months": f"파생변수 = 2023*12 - (일반_창업인수승계_연도*12 + 일반_창업인수승계_월). 창업연도가 {TENURE_YEAR_SENTINEL}(sentinel)인 행은 NaN(tenure_invalid_flag=1 참조). " + TENURE_MONTHS_CAVEAT,
    "tenure_invalid_flag": f"파생변수. 일반_창업인수승계_연도 == {TENURE_YEAR_SENTINEL}(sentinel)이라 tenure_months를 계산하지 않은 행 표시. 1=계산 불가.",
    "profit_margin": "파생변수 = 경영_영업이익 / 경영_매출금액. 매출 0이면 NaN, data_flag=1로 표시(필터 후 실제 발생 0건이지만 방어적으로 구현).",
    "data_flag": "profit_margin 계산 불가(매출 0) 행 표시 플래그. 1=계산 불가.",
    "is_seoul": f"파생변수 = (행정구역시도코드 == '{SEOUL_CODE_VALUE}'). 전국 5,042건 중 서울 474건(DATA_CATALOG.md §5)과 일치해야 함.",
    "경영_영업이익_winsorized": "경영_영업이익을 1%/99% 분위로 clip한 값. 원본 컬럼은 보존하고 모델링용으로 이 컬럼을 별도 제공.",
    "profit_outlier": "경영_영업이익 원본이 1%/99% 분위 밖이면 1. Winsorizing으로 조정된 행 표시.",
    "treat_binary": f"처치변수(이진, 최종). (경영_전자상거래_매출실적여부 == '{ECOMMERCE_YES_VALUE}')."
    f" 분포 assert: 1={EXPECTED_TREAT_COUNTS[1]}, 0={EXPECTED_TREAT_COUNTS[0]}.",
    "treat_cont": "처치변수(연속, 최종). treat_binary==0인 행은 경영_전자상거래_매출비율을 명시적 0으로 채움(NaN 아님).",
    "경영_부채여부_bin": f"경영_부채여부의 0/1 변환. {YESNO_YES_VALUE}(예)->1, {YESNO_NO_VALUE}(아니오)->0.",
    "창업_준비활동_사업계획서작성여부_bin": f"창업_준비활동_사업계획서작성여부의 0/1 변환. {YESNO_YES_VALUE}(수행)->1, {YESNO_NO_VALUE}(미수행)->0.",
    "창업_준비활동_시장조사여부_bin": f"창업_준비활동_시장조사여부의 0/1 변환. {YESNO_YES_VALUE}(수행)->1, {YESNO_NO_VALUE}(미수행)->0.",
    "창업_준비활동_동종업종종사경험여부_bin": f"창업_준비활동_동종업종종사경험여부의 0/1 변환. {YESNO_YES_VALUE}(수행)->1, {YESNO_NO_VALUE}(미수행)->0.",
    "창업_준비활동_창업교육여부_bin": f"창업_준비활동_창업교육여부의 0/1 변환. {YESNO_YES_VALUE}(수행)->1, {YESNO_NO_VALUE}(미수행)->0.",
}
